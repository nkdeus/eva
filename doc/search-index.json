[{"u":"index.html","p":"Getting started","h":"","x":"EVA CSS turns a static design into a responsive system without a single breakpoint. You give it the sizes your design actually uses; it gives you back CSS custom properties that scale continuously with the viewport, and an OKLCH color system where a whole theme is a handful of numbers. It is a SCSS package. There is no runtime, no JavaScript, no build plugin — you compile it with sass and ship the CSS."},{"u":"index.html#the-two-ideas","p":"Getting started","h":"The two ideas","x":"Everything in EVA comes from two decisions. Sizes are clamp() formulas, not values. A design says a card has 32px of padding. On a phone that is too much; on a 4K screen it is too little. EVA emits --32 as a clamp() that interpolates between a floor and a ceiling as the viewport widens. You write padding: var(--32) once and never write a media query for it. Colors are three numbers, not a palette. Each of the five base roles stores a lightness, a chroma and a hue, and every variant — opacity fades, brightness steps, the whole dark mode — is recomposed from those three at runtime in OKLCH. Swapping a theme means swapping fifteen numbers."},{"u":"index.html#quickstart","p":"Getting started","h":"Quickstart","x":"Three commands and a config block. Full detail in Installation and Configuration. 16 is mandatory in $sizes — EVA uses it as the rem reference and throws a compile error without it."},{"u":"index.html#what-you-get","p":"Getting started","h":"What you get","x":""},{"u":"index.html#where-to-go-next","p":"Getting started","h":"Where to go next","x":"- Starting a new project — read Installation, then Configuration. - Adding EVA to a codebase that already ships — go straight to Adopting EVA. The audit step there is what separates a clean migration from freezing years of design drift into named tokens. - Looking for one specific variable or class — Reference is the flat list. - Want to see it move — the Fluid CSS and Colors pages have live, resizable demos."},{"u":"install.html","p":"Installation","h":"","x":"EVA ships as three independent npm packages. Only the first is required. The only peer requirement is a SCSS toolchain. sass on its own is enough; Vite, Astro, Next and friends already bundle one."},{"u":"install.html#pick-an-entry-point","p":"Installation","h":"Pick an entry point","x":"eva-css-fluid exposes four entry points. They differ in how much they emit, not in what they can do. eva-css-fluid/colors takes no configuration — it has no sizes to generate. The other three accept the full option set described in Configuration."},{"u":"install.html#compile","p":"Installation","h":"Compile","x":"EVA is plain SCSS, so the reference command is plain sass. The --load-path is what lets @use 'eva-css-fluid' resolve from nodemodules. Wire the two useful variants into package.json: Use --style expanded while developing: the emitted clamp() formulas are the thing you inspect in DevTools, and compressed output makes them unreadable. Compress at the end, or let eva-purge do it."},{"u":"install.html#project-layout","p":"Installation","h":"Project layout","x":"Nothing is imposed. A single-target project usually looks like this: When several designs live in one repo, give each its own entry file and its own $sizes — the whole point of listing sizes explicitly is that each target only carries what it uses."},{"u":"install.html#mark-up-the-page","p":"Installation","h":"Mark up the page","x":"Two classes on the root element switch the whole system on. - current-theme — required. It is the element the color variables are computed on. - theme-<name — the active palette. See Theme configuration. - toggle-theme — add it to flip to dark mode. See Dark mode. - all-grads — add it if you use the gradient classes. See Gradients. Put current-theme on <html rather than <body if the page can over-scroll. The <html background fills the bounce area on mobile; a themed <body leaves an unthemed strip visible during pull-to-refresh. Next: Configuration."},{"u":"config.html","p":"Configuration","h":"","x":"Configuration is a single @use ... with (...) block. There is no config file, no CLI step, no plugin — sass reads it and emits the stylesheet. SCSS requires the lists to be parenthesised. $sizes: (4, 8, 16) compiles; $sizes: 4, 8, 16 is a parser error."},{"u":"config.html#options","p":"Configuration","h":"Options","x":"$unit-fluid, $reference-width, $fluid-runtime and $min-font-size arrived in 2.2.0. eva-css-fluid/colors accepts none of these — it generates no sizes."},{"u":"config.html#choosing-sizes","p":"Configuration","h":"Choosing $sizes","x":"List what your design uses. Nothing else. The list drives the output size directly: every entry produces four sizing tokens, and with $build-class: true, sixteen properties × four variants of utility classes. Two constraints: - 16 is required. It is the rem reference. Leave it out and the compile fails with Size 16 is required as a base size. - Neither list may be empty. Some values should never enter the list: Retrofitting an existing codebase is a different exercise: derive the list from what is actually in the CSS instead of writing it by hand. That workflow is Adopting EVA."},{"u":"config.html#the-two-build-modes","p":"Configuration","h":"The two build modes","x":"$build-class is the one option that changes how you write markup. Pick one per project and hold it."},{"u":"config.html#utility-classes-build-class-true","p":"Configuration","h":"Utility classes — $build-class: true","x":"EVA emits classes for sixteen properties across every size, plus color, flex, grid and layout utilities. Fast to write, and the markup carries the design. The tradeoff is stylesheet weight — which is what eva-purge exists to solve."},{"u":"config.html#variables-only-build-class-false","p":"Configuration","h":"Variables only — $build-class: false","x":"EVA emits nothing but custom properties. You write ordinary CSS. Better for design systems, component libraries, and anything dropped into a codebase that already has class names. eva-css-fluid/variables and eva-css-fluid/core force this mode. Don't mix the two in one project. Half the components reading utility classes and half reading semantic CSS is the fastest way to lose track of where a value comes from."},{"u":"config.html#trimming-the-class-output","p":"Configuration","h":"Trimming the class output","x":"With $custom-class: true, $class-config restricts generation per property. Only the sizes you list are emitted for that property; unlisted properties are skipped entirely. The keys must be real property prefixes — w mw h p px pr py br mb mr ml mt pt pb g gap. Anything else is a compile error. The variables are unaffected: var(--32) still exists even when no w-32 class does."},{"u":"config.html#static-escape-hatches","p":"Configuration","h":"Static escape hatches","x":"$px-rem-suffix: true adds a fixed counterpart to each size: Useful in two situations: debugging a fluid value against its design reference, and the handful of dimensions that must not scale. With $build-class: true you also get w-32-px, p-32-rem and so on."},{"u":"config.html#fluid-unit-and-accessibility-floor","p":"Configuration","h":"Fluid unit and accessibility floor","x":"Since 2.2.0 the fluid unit is a runtime variable rather than a baked-in literal. Configure the default in SCSS, override it per subtree in CSS. This is covered in full under Fluid sizing. $min-font-size is the mobile floor, not the desktop size. 13–14 is a reasonable value; 16 would flatten the scale. Next: Fluid sizing."},{"u":"sizes.html","p":"Fluid sizing","h":"","x":"Every value in $sizes becomes four CSS custom properties. Every value in $font-sizes becomes three, under a separate prefix. None of them is a fixed number — each is a clamp() that interpolates between a floor and a ceiling as the viewport widens. All four peak at the same ceiling. What separates them is how fast they get there — and therefore how small they become on a phone."},{"u":"sizes.html#the-four-variants","p":"Fluid sizing","h":"The four variants","x":"Measured, for $sizes containing 16, at a 16px root font size: Read the first column, not the third: at desktop width every variant lands on the design value, so the choice is entirely about what happens on a phone. --16 gives back 8px of space there; --16- keeps 14.6px. A practical default: var(--N) for widths, heights and layout dimensions, var(--N-) for the paddings and gaps inside components. Cramped mobile spacing is almost always a var(--N) that should have been var(--N-). The rule generalises: the more suffix characters, the more aggressive the collapse. --32 behaves to --32 exactly as --16 does to --16."},{"u":"sizes.html#font-sizes","p":"Fluid sizing","h":"Font sizes are a separate namespace","x":"This is the single most common source of confusion. Font tokens carry an fs- prefix and live in their own list. The same number in both lists produces two different tokens that clamp differently. 44 in $font-sizes gives you var(--fs-44) and no var(--44) at all — and an undefined custom property resolves to nothing, so the declaration is silently dropped and the browser keeps its default. Nothing errors; the page just looks slightly wrong. Font sizes have three variants rather than four, and the scale is shifted one notch toward safety: var(--fs-16) on a 375px screen renders 13.1px — readable. var(--fs-16) renders 7.1px, which is not body copy. Use the unsuffixed variant for anything a reader has to read, and keep and for display type that has room to shrink."},{"u":"sizes.html#fluid-unit","p":"Fluid sizing","h":"Fluid unit: viewport or container","x":"Since 2.2.0 the unit is not baked into the compiled clamp(). Every token multiplies a custom property, --eva-fluid-unit, which defaults to 1vw. Override it on any element and that subtree changes what it measures against — no rebuild, no second stylesheet. Two utility classes do both halves at once, so you cannot forget one: Reach for cqi when a component's size should follow the box it is placed in rather than the page — a card that must look identical in a full-width hero and in a narrow sidebar, a design-system preview panel, a widget embedded at an unknown width. Keep the default vw for page-level type and spacing, which genuinely should respond to the whole viewport. Set the project-wide default in SCSS: $fluid-runtime: false reverts to the pre-2.2.0 literal output, byte for byte. Only useful for diffing against an old build — it removes the ability to switch units at runtime."},{"u":"sizes.html#accessibility-floor","p":"Fluid sizing","h":"Accessibility floor","x":"$min-font-size raises the lower bound of every font-size clamp. It is expressed in px and converted to rem, so it still follows browser zoom. The floor is the mobile size, not the desktop one. 13–14 is a sane value. Setting it to 16 flattens the scale: every token would start at its ceiling and stop being fluid at all. Default is 0, which disables the floor entirely."},{"u":"sizes.html#reference-width","p":"Fluid sizing","h":"Reference width","x":"$reference-width (default 1440) is the viewport at which tokens reach their design value. Lower it for a design drawn at 1280, raise it for one drawn at 1920 — the whole scale shifts with it."},{"u":"sizes.html#static-counterparts","p":"Fluid sizing","h":"Static counterparts","x":"With $px-rem-suffix: true, each size also emits a fixed pair: Two uses: comparing a fluid value against its design reference in DevTools, and the small set of dimensions that must not scale — border widths, touch targets, pill radii. For those, a literal 48px is just as good and clearer about intent."},{"u":"sizes.html#checking-your-work","p":"Fluid sizing","h":"Checking your work","x":"You cannot read a clamp() and predict how it feels. Build a throwaway page that renders every token at every variant as a real square, and resize the browser. An empty string means the token does not exist — either the size is missing from $sizes, or you asked for a sizing token when you meant --fs-. That check catches most configuration mistakes in seconds. The Fluid CSS and Sizes pages run exactly this experiment live, if you would rather resize a window than build one. Next: Colors."},{"u":"colors.html","p":"Colors","h":"","x":"EVA does not store colors. It stores three numbers per role and recomposes everything in OKLCH at runtime. OKLCH is what makes this work: it is perceptually uniform, so shifting lightness does not drag the hue with it. A darker orange stays orange. That is not true in HSL."},{"u":"colors.html#the-five-roles","p":"Colors","h":"The five roles","x":"The defaults are deliberately colorless — a theme is what gives them values. dark and light inherit --brand-hue and carry a small chroma. Ink and background are therefore tinted by the brand. It is a design choice, not an oversight, and it surprises people: change --brand-hue to test a blue and every paragraph shifts with it. To opt out, set --dark-chroma and --light-chroma to 0, or pin --dark-hue and --light-hue explicitly."},{"u":"colors.html#variants","p":"Colors","h":"Variants","x":"Each role produces eight variables. Two independent axes: opacity and lightness. The same pattern applies to accent, extra, dark and light. The two axes do not cross. There is no --brand-d — no 35% version of a brightness step. Fades are inlined at build time from $fade-values; brightness steps are recomputed at runtime from --<role-lightness. It is a known limit of the system, not a naming gap."},{"u":"colors.html#brightness-steps","p":"Colors","h":"Brightness steps","x":"Each step is computed from the role's lightness with one formula: Four global offsets drive the four steps, and they flip with the mode: --darker being positive in dark mode is not a bug. In dark mode the ink role is light (95%), and -d means \"more contrast against the background\", not \"darker in absolute terms\". The steps describe a relation. That is also why the bounds swap with the mode."},{"u":"colors.html#per-role-tuning-2-4-0","p":"Colors","h":"Per-role tuning (2.4.0+)","x":"Before 2.4.0 those four offsets were global: every role got the same step. Since 2.4.0 each role can override its own, and each step can take a proportional share of the remaining headroom instead of a fixed offset. <role is brand, accent, extra, dark or light. <token is darker, brighter, darker or brighter. Why the ratio exists. OKLCH lightness is clamped to 0%–100%. With --light-lightness: 96.4%: Two steps, one color. And it is symmetric: in dark mode --dark-d and --dark-d collapse together on black. On every neutral, in every mode, two of the four steps were unusable — precisely on dark and light, the two most-used roles. Measured in the browser, in OKLCH lightness, before and after switching to proportional:"},{"u":"colors.html#recipes","p":"Colors","h":"Recipes","x":"Tight neutrals, wide accent — only the named step moves, --dark-b keeps the global value: Steps that never saturate — an absolute part of 0 makes the step purely proportional, and it holds in both modes: Both terms add up — a guaranteed minimum plus a share of what is left: These are plain custom properties. Set them on .current-theme, or on any nested element that also carries .current-theme, to preview a variation in place. Fully backward compatible. While a per-role token is unset the native var() fallback lands on the original global value, and an unset ratio is 0, so its term cancels. Upgrading changes no computed value."},{"u":"colors.html#themes","p":"Colors","h":"Themes","x":"A theme is a CSS class that sets the OKLCH triplets. Nothing else. Convert your existing HEX palette with the eva-color CLI:"},{"u":"colors.html#several-themes-at-once","p":"Colors","h":"Several themes at once","x":"Declare as many .theme-<name blocks as you need and swap the class. Seasonal palettes, brand variants, per-customer themes — all the same mechanism. Two things worth knowing: - One source of truth per theme. Set only the triplets. Never redeclare --brand itself; let EVA derive it. - Preview swatches scope cleanly. To show a non-active theme's color, wrap the swatch in <div class=\"current-theme theme-december\" and background: var(--brand) resolves to that theme's color inside the card, whatever the page is running. Keep --light-chroma low — around 0.005 to 0.015. Higher reads as saturated and breaks the neutral-surface feel."},{"u":"colors.html#dark-mode","p":"Colors","h":"Dark mode","x":"EVA ships dark mode. Do not build your own with .dark classes and HEX overrides. Two variables decide the mode, and they are the only place it is decided: light reads the first, dark reads the second. Adding .toggle-theme swaps them: var(--light) is bright in light mode and dark in dark mode. var(--dark) is the opposite. The two roles literally swap — that is the whole trick, and it is why no color needs redefining. The !important on the two lightness lines means a theme's configured --dark-lightness and --light-lightness are overridden in dark mode. Only the hue and chroma of dark and light survive the switch. That is intentional — it is what guarantees contrast — but it catches people who tuned those values per theme."},{"u":"colors.html#bridging-semantic-tokens","p":"Colors","h":"Bridging semantic tokens","x":"If your codebase already has --color-bg, --color-text and friends, alias them onto EVA rather than replacing them. They then flip with .toggle-theme and stay tinted by the active theme for free. If the app boots client-side and applies the theme classes after mount, keep a static :root { --color-bg: fafaf7; } fallback to avoid a flash of unthemed content on first paint."},{"u":"colors.html#color-utility-classes","p":"Colors","h":"Color utility classes","x":"With $build-class: true, EVA emits 200 color classes — five properties × five roles × eight variants. The Colors and Auto theme pages let you drag the hue and watch all 200 update live. Next: Gradients."},{"u":"gradients.html","p":"Gradients","h":"","x":"A gradient in EVA is not one class, it is a small stack: an applier that draws it, and setters that feed it. Everything travels through three custom properties, so any part can be overridden anywhere. The container must carry all-grads. That class is what declares the three variables; without it the appliers have nothing to read."},{"u":"gradients.html#the-three-variables","p":"Gradients","h":"The three variables","x":"Set them by hand when the class vocabulary is not enough:"},{"u":"gradients.html#appliers","p":"Gradients","h":"Appliers","x":"The -text variants set background-clip: text and a transparent fill. The -border variants use border-image, so the element needs a border-style and a border-width for anything to show."},{"u":"gradients.html#color-setters","p":"Gradients","h":"Color setters","x":"from- and to- accept every color role and every one of its variants — the same vocabulary as the color system. Because the endpoints are theme variables, a gradient re-derives itself on theme switch and in dark mode. Nothing to redeclare."},{"u":"gradients.html#direction","p":"Gradients","h":"Direction","x":"Eight shorthands, and full angle control when you need it. a-0 through a-360 in 5° steps — 73 classes: a-45, a-90, a-135, a-215, a-360. d- and a- both declare --current-angle with !important. Do not put both on one element — the winner is whichever comes later in the stylesheet, not in your class attribute."},{"u":"gradients.html#zoom-and-position","p":"Gradients","h":"Zoom and position","x":"Enlarging the gradient box is what makes the animation readable — a gradient at 100% has nowhere to travel."},{"u":"gradients.html#animation","p":"Gradients","h":"Animation","x":"All three run the same gradient-shift keyframes, which pans the background position. Pair them with a bg-size class or nothing will appear to move."},{"u":"gradients.html#putting-it-together","p":"Gradients","h":"Putting it together","x":"The Gradients page has the whole vocabulary as a live playground. Next: Utility classes."},{"u":"utilities.html","p":"Utility classes","h":"","x":"Everything on this page exists only when $build-class: true. With eva-css-fluid/variables or eva-css-fluid/core you get the same values as custom properties and write your own selectors."},{"u":"utilities.html#sizing","p":"Utility classes","h":"Sizing","x":"Sixteen property prefixes, each combined with every entry in $sizes and each of the four scaling variants. The variant suffix comes last, exactly as on the variable: With $px-rem-suffix: true you also get the static pair — p-32-px, w-64-rem. There is no m-, mx- or my-. Margins are only emitted per side. And note that px-/py- map to the logical properties padding-inline / padding-block, so they follow writing direction."},{"u":"utilities.html#font-size","p":"Utility classes","h":"Font size","x":"fs-N for every entry in $font-sizes, with the two shrink variants: Remember these read the --fs- namespace, which is separate from $sizes. See Font sizes are a separate namespace."},{"u":"utilities.html#colors","p":"Utility classes","h":"Colors","x":"Five properties × five roles × eight variants — 200 classes. The leading underscore is deliberate: it keeps EVA's color classes from colliding with anything already in your codebase. Two shadow helpers ship alongside: shadow and shadow."},{"u":"utilities.html#flexbox","p":"Utility classes","h":"Flexbox","x":".flex is the container. .x and .y set the axis; without either, the direction is row. Alignment shorthands, valid on their own or combined with x / y: Inside .x or .y the two axes can be set independently with a compound name — justify-align: Both halves accept start, center, end, space, around, evenly on the main axis, and start, center, end, stretch, baseline on the cross axis. The explicit form works too: justify-center, items-end, content-space. Item-level classes: Two composed patterns are included: .flex-card (header / body / footer, body grows) and .flex-sidebar (.flex-sidebar-nav fixed, .flex-sidebar-main grows with min-width: 0)."},{"u":"utilities.html#grid","p":"Utility classes","h":"Grid","x":"Two independent systems."},{"u":"utilities.html#auto-fit-grid","p":"Utility classes","h":"Auto-fit grid","x":"auto-fit-<size<variant sets the minmax() floor of a repeat(auto-fit, …) template. The column count follows the available width by itself — no breakpoints. auto-fit- classes are only generated for sizes ≥ 100. A auto-fit-64 will not exist even if 64 is in $sizes, because a 64px column floor is not a useful grid."},{"u":"utilities.html#twelve-column-flex-grid","p":"Utility classes","h":"Twelve-column flex grid","x":"Breakpoints for the responsive form: xs 200px, sm 500px, md 700px, lg 1000px, xl 1200px, xxl 1400px."},{"u":"utilities.html#typography","p":"Utility classes","h":"Typography","x":"Variable-font axis classes, driven by two custom properties: lh-1-5 does not exist. For any other line height, declare it yourself."},{"u":"utilities.html#layout","p":"Utility classes","h":"Layout","x":""},{"u":"utilities.html#runtime-containers","p":"Utility classes","h":"Runtime containers","x":"See Fluid unit."},{"u":"utilities.html#trimming-the-output","p":"Utility classes","h":"Trimming the output","x":"All of this compiles to a large stylesheet. Two ways to cut it down: - $custom-class: true with $class-config restricts generation up front — see Configuration. - eva-purge removes what your markup never uses, after the fact. Typically 40–70%. Next: Adopting EVA."},{"u":"adopt.html","p":"Adopting EVA","h":"","x":"EVA slots into an existing codebase without renaming a single class. You keep your CSS; you swap fixed px for var(--N). What changes is where the numbers come from. The order of operations depends on when you arrive. The retrofit case is the common one, and the audit is what separates a clean migration from freezing years of accidental design drift into named tokens. Do not skip it."},{"u":"adopt.html#audit","p":"Adopting EVA","h":"Audit and consolidate","x":""},{"u":"adopt.html#count-every-px","p":"Adopting EVA","h":"Count every px","x":"Do not write the $sizes list by hand. Read it out of the codebase. A mid-size codebase typically lands at 50 to 80 distinct px values. Sorted by frequency, the top twenty are real tokens and the long tail is noise."},{"u":"adopt.html#decide-what-stays-out","p":"Adopting EVA","h":"Decide what stays out","x":"Some sizes should never be fluid. Cut them before you start. Drawing the EVA band at roughly 6–600px removes about 30% of the audit noise immediately."},{"u":"adopt.html#fuse-near-duplicates","p":"Adopting EVA","h":"Fuse near-duplicates","x":"Inside the band, look for adjacent values where one clearly dominates: Heuristics that hold up in practice: - 1–2px gap — almost always fuse. - 3–4px gap — fuse if one side is used at least 5× more than the other. - Singletons — fuse to the nearest neighbour, or drop entirely. - Ties — pick the rounder number. Either is fine. A real retrofit went from 61 to 49 distinct values, 27 line edits across 9 files. The visual diff was imperceptible."},{"u":"adopt.html#apply-with-a-script-never-by-hand","p":"Adopting EVA","h":"Apply with a script, never by hand","x":"Read the dry run before applying. The regex matches every occurrence, including ones that are not size tokens: A 4px difference in a shadow blur is invisible, so most catches are harmless — but look once."},{"u":"adopt.html#re-audit-then-list","p":"Adopting EVA","h":"Re-audit, then list","x":"Run the audit again. What survives inside the band becomes $sizes. A font-size-scoped variant of the same script (/font-size\\s:\\s([^;]+)/) gives you $font-sizes."},{"u":"adopt.html#alias-your-design-tokens","p":"Adopting EVA","h":"Alias your design tokens","x":"If the project already has SCSS tokens, point them at EVA. Every component that reads $space-lg becomes fluid with no component edits at all — this is the lowest-friction migration there is. Four categories should never become fluid: touch targets (44–48px minimum regardless of viewport), body font size below its readable floor, border widths, and pill radii."},{"u":"adopt.html#layout","p":"Adopting EVA","h":"Layout","x":"The point is that components stop carrying breakpoints. Padding, gaps and type all breathe on their own. You still need media queries for structural change — one column becoming a sidebar layout. EVA does not replace those, and it should not. Media queries for topology. Never for spacing or typography."},{"u":"adopt.html#verify-visually","p":"Adopting EVA","h":"Verify visually","x":"EVA emits clamp() formulas, and you cannot intuit how aggressive a variant is from the math. Build a throwaway /dev route with one card per token, each showing all four variants as real squares, and resize the browser. It pays for itself three times over: 1. Smoke test. An empty value means the @use ... with (...) block is missing or scoped wrong. 2. Variant choice. Comparing var(--16) next to var(--16-) at 320px is the only honest way to decide which one a component wants. 3. Onboarding. Designers can check the scale without opening component code."},{"u":"adopt.html#traps","p":"Adopting EVA","h":"Traps","x":"Error: expected \"$\" in the @use ... with () block. Wrap the list in parentheses: $sizes: (4, 8, 16), not $sizes: 4, 8, 16. Size 16 is required as a base size. Add 16 to $sizes. It is the rem reference. Mobile spacing feels cramped. You used var(--16) where you wanted var(--16-). The conservative variant keeps a much higher floor on small screens. Body text shrinks too far. Use the unsuffixed var(--fs-16). The and variants are display type, not copy. var(--44) resolves to nothing even though 44 is in $font-sizes. Font tokens are --fs-44. The two namespaces are independent — the same number is not the same token. var(--15) silently resolves to nothing after consolidation. Removing 15 from $sizes does not remove var(--15) from your code. An undefined custom property makes the browser drop the declaration and fall back to its default, so the page still renders and the bug stays invisible. After fusing, grep for the tokens you removed. Component CSS beats an EVA media query. With equal specificity, source order decides. A default written after the media query wins at every viewport: Move the default above the query, or raise its specificity."},{"u":"adopt.html#colors-or-not","p":"Adopting EVA","h":"Colors, or not","x":"You do not have to take the color system. EVA's value is in the fluid sizing; the colors are a separate offer. If you already have a working palette and theming, keep it and adopt OKLCH later — eva-css-fluid/variables gives you sizes and colors, and you can simply ignore the color half. If you do take it, convert your palette in one command and read Colors: Next: CLI tools."},{"u":"cli.html","p":"CLI tools","h":"","x":"Two optional packages sit beside the framework. Neither is needed to compile EVA — they handle the two jobs around it: getting colors in, and getting unused CSS out."},{"u":"cli.html#eva-color","p":"CLI tools","h":"eva-color","x":"Five commands. Everything reads and writes plain HEX or OKLCH, so it fits any pipeline."},{"u":"cli.html#convert","p":"CLI tools","h":"Converting a palette","x":"Those three numbers are exactly what a theme block wants: to-hex goes the other way, which is what you want when handing a value back to a designer working in HEX:"},{"u":"cli.html#generating-a-palette","p":"CLI tools","h":"Generating a palette","x":"Seven perceptually even steps from one seed. Because the steps are computed in OKLCH, the hue holds across the whole ramp — a dark step of a green stays green instead of drifting olive the way an HSL ramp does."},{"u":"cli.html#generating-a-whole-theme","p":"CLI tools","h":"Generating a whole theme","x":"Describe the five roles in JSON: Out comes a .theme-myproject block with all fifteen values, ready to paste into your SCSS. A generated theme writes the hues explicitly, which incidentally opts you out of the brand-tinted neutrals described in Colors."},{"u":"cli.html#checking-contrast","p":"CLI tools","h":"Checking contrast","x":"Worth running on the pairs your theme actually produces — --dark on --light, --light on --brand — rather than on the seed colors. The brightness steps move lightness around, and a step that reads fine in light mode can fall under the threshold once the mode flips."},{"u":"cli.html#from-javascript","p":"CLI tools","h":"From JavaScript","x":"The same functions are importable: Useful for generating themes at build time — per customer, per season, per brand — instead of hand-writing a block for each."},{"u":"cli.html#purge","p":"CLI tools","h":"eva-purge","x":"$build-class: true emits every combination of every property and every size. Most projects use a fraction of that. eva-purge scans your markup and drops the rest — typically 40 to 70% of the file. It knows about EVA specifically, so it does not break what makes the framework work: - every custom property in :root survives, because a purged class may still be referenced by hand-written CSS; - element selectors (body, h1, p) are kept; - media queries are preserved; - theme classes are protected through the safelist."},{"u":"cli.html#safelist-what-javascript-adds","p":"CLI tools","h":"Safelist what JavaScript adds","x":"The scanner reads your markup, so any class only ever added at runtime is invisible to it. Anything toggled by script must be safelisted: For anything more involved, a config file takes regular expressions: Purge at the end of the build and ship the purged file. Keep pointing your dev pages at the full stylesheet — otherwise a class you add while working silently does nothing until the next purge."},{"u":"cli.html#wiring-it-up","p":"CLI tools","h":"Wiring it up","x":"The other lever is $custom-class — see Configuration. Restricting generation up front and purging afterwards are complementary, not alternatives: the first keeps the dev stylesheet small, the second keeps the shipped one small. Next: Reference."},{"u":"reference.html","p":"Reference","h":"","x":"Everything EVA emits, in one place. Version 2.4.0."},{"u":"reference.html#configuration","p":"Reference","h":"Configuration","x":""},{"u":"reference.html#entry-points","p":"Reference","h":"Entry points","x":""},{"u":"reference.html#size-tokens","p":"Reference","h":"Size tokens","x":"For each N in $sizes: For each N in $font-sizes: Runtime unit: --eva-fluid-unit, default 1vw, override to 1cqi per subtree. Classes .eva-cqi and .eva-root."},{"u":"reference.html#color-tokens","p":"Reference","h":"Color tokens","x":"Roles: brand, accent, extra, dark, light. Theme inputs, per role: --<role-lightness, --<role-chroma, --<role-hue. Mode pivots: --current-lightness (96.4% light / 5% dark), --current-darkness (6.4% / 95%). Brightness formula: Per-role overrides, with <token in darker, brighter, darker, brighter: Body classes: current-theme (required), theme-<name, toggle-theme (dark), all-grads (gradients)."},{"u":"reference.html#utility-classes","p":"Reference","h":"Utility classes","x":"Sizing — w mw h p px py pt pb pr mt mb ml mr g gap br, each × every size × (none) -. Font size — fs-N, fs-N, fs-N. Color — c- bg- bc- f- s- × 5 roles × 8 variants. Plus shadow, shadow. Flex — flex, x, y, reverse, wrap, nowrap, wrap-reverse; start center end space around evenly; compound justify-align forms; justify-, items-, content-; flex-1 flex-2 flex-3 flex-auto flex-initial flex-none stretch; self-; order-1…order-12, order-first, order-last, order-none; flex-card, flex-sidebar. Grid — grid, auto-fit-<size<variant (sizes ≥ 100 only), flex-grid, container:flex-grid, col-1…col-12, col-1/1…col-1/12, max-col-1…max-col-12, xs:max-col-N…xxl:max-col-N. Gradients — grad-linear, grad-radial, grad-linear-text, grad-radial-text, grad-linear-border, grad-radial-border; from-, to-, from-transparent, to-transparent; d-t d-b d-l d-r d-tl d-tr d-bl d-br; a-0…a-360 in 5° steps; bg-size, bg-size, bg-size; bg-center bg-top bg-bottom bg-left bg-right; animated, animated-slow, animated-fast. Typography — fwg-1…fwg-8, fwd-1…fwd-13, f-scale, f-scale offset, bold, ttu, tac, lh-0, lh-1. Layout — por poa pof pos, poa center, w-full, h-full, oh, ar-1, circle, border, border thin, blur blur blur, pointer, "},{"u":"reference.html#cli","p":"Reference","h":"CLI","x":""},{"u":"reference.html#rules","p":"Reference","h":"Rules","x":"1. No fixed values. var(--64), not 64px. var(--brand), not ff5733. 2. Only list the sizes the design actually uses. 3. 16 is mandatory in $sizes. 4. Font tokens are --fs-N. $sizes and $font-sizes are independent namespaces. 5. Pick one build mode per project and hold it. 6. current-theme on the root element, always. 7. all-grads on the container if you use gradients. 8. Media queries for layout topology only — never for spacing or type."},{"u":"reference.html#known-limits","p":"Reference","h":"Known limits","x":"- The two color axes do not cross. No opacity variant of a brightness step; no --brand-d. - Fades are inlined at build time. $fade-values (65 / 35 / 15%) is a SCSS variable, not a runtime one — no per-theme control. - dark and light inherit --brand-hue with a non-zero chroma, so neutrals are brand-tinted unless you pin them. - Dark mode overrides configured lightness. .toggle-theme sets --dark-lightness and --light-lightness with !important; only hue and chroma survive the switch. - auto-fit- exists only for sizes ≥ 100."},{"u":"reference.html#links","p":"Reference","h":"Links","x":"- GitHub - eva-css-fluid on npm - eva-colors on npm - eva-css-purge on npm"}]